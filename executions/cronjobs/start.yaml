apiVersion: batch/v1
kind: CronJob
metadata:
  name: my-job
spec:
  startingDeadlineSeconds: 17
  jobTemplate:
    metadata:
      name: my-job
    spec:
      template:
        metadata: {}
        spec:
          containers:
          - command:
            - /bin/sh
            - -c
            - date; echo hello from the kubernetes cluster
            image: busybox
            name: my-job
            resources: {}
          restartPolicy: Never
  schedule: '*/1 * * * *'
status: {}
